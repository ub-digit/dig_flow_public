<% content_for :title do %>
  <ul class="breadcrumb">
    <li><%= link_to t("users.index.header"), users_path %><span class="divider">/</span></li>
    <li><%= link_to @user.name, user_path(@user.id) %><span class="divider">/</span></li>
    <li class="active"> <%= t("users.edit.header") %> </li>
  </ul>
<% end %>

<div id="edit_form">
<%= render :partial => 'user_form' %>
</div>
